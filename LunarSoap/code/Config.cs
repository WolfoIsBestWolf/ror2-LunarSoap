/*
using BepInEx;
using BepInEx.Configuration;

namespace LunarSoap
{
    public class WConfig
    {
        public static ConfigFile ConfigFileUNSORTED = new ConfigFile(Paths.ConfigPath + "\\Wolfo.LunarSoap.cfg", true);

        //public static ConfigEntry<bool> VariantAffectMoveSpeed;

        public static ConfigEntry<float> SpeedMult;
        public static ConfigEntry<float> SpeedMultStack;
        public static ConfigEntry<float> AccelerationMult;
        public static ConfigEntry<float> AccelerationMultStack;
        public static ConfigEntry<float> AccelerationMultSprinting;

        public static void InitConfig()
        {
            /*VariantAffectMoveSpeed = ConfigFileUNSORTED.Bind(
                "Item",
                "Affect Movement Speed stat instead of Walkspeed Coeef",
                false,
                "Movement speed stat affects the distance of utility skills."
            );
            SpeedMult = ConfigFileUNSORTED.Bind(
                "Stats",
                "Speed Multiplier",
                2f,
                "First stack"
            );
            SpeedMultStack = ConfigFileUNSORTED.Bind(
                "Stats",
                "Speed Multiplier (Per Stack)",
                0.4f,
                "Not counting first stack"
            );

            AccelerationMult = ConfigFileUNSORTED.Bind(
                "Stats",
                "Acceleration Multiplier",
                0.2f,
                "First stack"
            );
            AccelerationMultStack = ConfigFileUNSORTED.Bind(
                "Stats",
                "Acceleration Multiplier (Per Stack)",
                0.2f,
                "Not counting first stack"
            );
            AccelerationMultSprinting = ConfigFileUNSORTED.Bind(
                "Stats",
                "Acceleration extra Multiplier (Sprinting)",
                0.75f,
                "Extra multiplier when sprinting"
            );


        }

    }
}
*/